import { MxDirective } from './mx.directive';

describe('MxDirective', () => {
  it('should create an instance', () => {
    const directive = new MxDirective();
    expect(directive).toBeTruthy();
  });
});
