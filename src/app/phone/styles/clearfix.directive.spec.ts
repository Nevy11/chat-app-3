import { ClearfixDirective } from './clearfix.directive';

describe('ClearfixDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearfixDirective();
    expect(directive).toBeTruthy();
  });
});
