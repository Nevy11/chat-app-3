import { PyDirective } from './py.directive';

describe('PyDirective', () => {
  it('should create an instance', () => {
    const directive = new PyDirective();
    expect(directive).toBeTruthy();
  });
});
